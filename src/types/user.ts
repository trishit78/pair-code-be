export type UserDataDTO = {
    username:string,
    email:string,
    password:string
}

export type UserSigninDTO={
    email:string,
    password:string
}

export type inputDataDTO={
    id:string,
    email:string
}